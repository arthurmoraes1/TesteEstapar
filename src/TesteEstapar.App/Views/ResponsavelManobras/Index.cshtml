@model IEnumerable<TesteEstapar.App.ViewModels.ResponsavelManobraViewModel>

@{
    ViewData["Title"] = "Lista de Responsáveis";
}

<h1>@ViewData["Title"]</h1>
<hr />

<vc:Summary></vc:Summary>
<p>
    <a class="btn btn-info" asp-action="Create">Novo Manobrista</a>
</p>

<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Carro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manobrista)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Carro.Modelo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manobrista.Nome)
                </td>
                <td class="text-right">
                    <a class="btn btn-warning" asp-controller="ResponsavelManobras" asp-action="Details" asp-route-id="@item.Id"><spam class="fa fa-search"></spam> </a>
                    <a class="btn btn-info" asp-controller="ResponsavelManobras" asp-action="Edit" asp-route-id="@item.Id"><spam class="fa fa-pencil-alt"></spam> </a>
                    <a class="btn btn-danger" asp-controller="ResponsavelManobras" asp-action="Delete" asp-route-id="@item.Id"><spam class="fa fa-trash"></spam> </a>
                </td>
            </tr>
        }
    </tbody>
</table>
